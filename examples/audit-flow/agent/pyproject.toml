[project]
name = "audit-flow-agent"
version = "0.1.0"
description = "CLI Agent with Command Audit Log snapshot queries for AUI"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "websockets>=12.0",
    "python-dotenv>=1.0.1"
]
