[project]
name = "zustand-flow-agent"
version = "0.1.0"
description = "CLI Agent for testing Zustand bridge global state and callable actions"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "websockets>=12.0",
    "python-dotenv>=1.0.1"
]
