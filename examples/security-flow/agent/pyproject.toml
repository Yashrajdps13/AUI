[project]
name = "security-flow-agent"
version = "0.1.0"
description = "CLI Agent testing Write-Side Security Scoping guards"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "websockets>=12.0",
    "python-dotenv>=1.0.1"
]
