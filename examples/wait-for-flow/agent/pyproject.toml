[project]
name = "wait-for-flow-agent"
version = "0.1.0"
description = "CLI Agent testing the waitFor condition handshake protocol"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "websockets>=12.0",
    "python-dotenv>=1.0.1"
]
